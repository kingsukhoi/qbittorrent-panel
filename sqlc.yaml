version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "sqlc"
        out: "pkg/sqlc"
        overrides:
          - column: "torrents_files.is_seed"
            go_type: "bool"
